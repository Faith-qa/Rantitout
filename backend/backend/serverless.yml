org: faith
app: talk-to-me
service: backend
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs14.x


plugins:
  - serverless-offline


functions:
  api:
    handler: handler.handler
    events:
      - httpApi: '*'

custom:
  serverless-offline:
    httpPort: 5000